# 异常处理与响应机制

原先机制：Success、Error 和 ErrorCode

                ⬇️

新机制：纯原生的 FastAPI 抛异常 + 全局中间件拦截

# 业务模块

改造现有 example 示例模块，把原本分散在 api、services、repository 中的代码，物理合并到一个 routers 文件中

# 测试

原先机制：conftest.py + Factory + 依赖注入

                ⬇️

新机制：黑盒集成测试，AI 写一个简单的脚本用 httpx 去请求这个接口，校验返回结果

# 更新 README.md 与 docs 文档